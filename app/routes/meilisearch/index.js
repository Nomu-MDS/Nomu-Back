// Import and define meilisearch-related routes here
const express = require('express');
const router = express.Router();

// Example route
// router.use('/meilisearch', require('./someRoute'));

module.exports = router;